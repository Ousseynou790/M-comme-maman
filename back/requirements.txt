# Socle
Django==5.2.6
psycopg[binary]==3.3.4

# Hachage des mots de passe : le hacheur Argon2 de Django a besoin de ce paquet.
argon2-cffi==25.1.0

# API consommée par la vitrine et le back-office Next
djangorestframework==3.16.1
django-cors-headers==4.7.0

# Configuration par variables d'environnement, jamais en dur dans le dépôt
django-environ==0.12.0

# Validation des images envoyées dans la photothèque (ImageField).
Pillow==12.3.0

# Production. `runserver` ne sert qu'au développement : en ligne c'est gunicorn
# qui tient les requêtes, et WhiteNoise qui sert les fichiers statiques de
# l'admin Django sans dépendre d'un serveur de fichiers séparé.
gunicorn==23.0.0
whitenoise==6.11.0

# Photothèque sur Cloudflare R2 (compatible S3). `boto3` est le client, et
# `s3transfer` sa dépendance d'envoi — épinglée pour que la livraison soit
# reproductible.
django-storages==1.14.6
boto3==1.43.75
s3transfer==0.19.2
